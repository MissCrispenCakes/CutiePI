#pragma once
// Copy this file to secrets.h and fill in your values.
//   cp firmware/include/secrets.h.example firmware/include/secrets.h
//
// secrets.h is gitignored — never commit your actual credentials.
// Only needed for the MQTT build (pio run -e bear-mqtt).

#define WIFI_SSID     "your_network_name"
#define WIFI_PASSWORD "your_network_password"
#define MQTT_BROKER   "broker.hivemq.com"  // free public broker — fine for testing
#define MQTT_PORT     1883
#define MQTT_USER     ""  // leave empty for brokers that need no auth
#define MQTT_PASS     ""
